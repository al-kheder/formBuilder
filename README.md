
  # Form module V2 (Copy)

  This is a code bundle for Form module V2 (Copy). The original project is available at https://www.figma.com/design/KhSV98Bqn3DpFJCvdJDFju/Form-module-V2--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  